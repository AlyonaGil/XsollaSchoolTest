const Router = require('express');
const router = new Router();

router.post('/',)
router.get('/',)
router.get('/:id',)
router.get('/:sku',)
router.put('/:id',)
router.put('/:sku',)
router.delete('/:id')
router.delete('/:sku')

module.exports = router
